# First-person control tasklist

Persistent queue for autonomous slices that advance CityLife toward FPS-quality roaming. Each slice should finish one small executable item with tests and demo evidence when UI-visible.

## Tasks

1. [x] Pointer-lock/mouse-look first-person mode with ESC restore.
   - Acceptance: entering first-person can request pointer lock from a user gesture; mouse movement updates yaw/pitch within clamped limits; ESC/pointerlockchange exits mouse-look without leaving first-person stuck; covered by tests or isolated control math tests plus browser demo.
   - Done 2026-06-23 14:01 SAST: completed the remaining sensitivity polish with Low/Normal/High player-facing mouse-look presets. Runtime state now scales yaw/pitch deterministically from config tunables, the HUD shows the selected preset, and browser evidence selected High and measured yawDelta `0.16`, lookPitch `0.16`; screenshot `/Users/joehermesbot/.hermes/cache/screenshots/browser_screenshot_90f8e13b97104bf8982db963afbd2193.png`.
   - Slice done 2026-06-23 13:45 SAST: added a mouse-look comfort affordance, `Level view`, that zeros vertical pitch without snapping yaw/heading so operators can recover from looking too far up/down while staying immersed. Covered by RED/GREEN runtime and HUD regressions plus browser probe/screenshot. Remaining for full task: optional sensitivity slider or persisted preference.
   - Slice done 2026-06-23 13:05 SAST: first-person now surfaces pointer-lock denial/unavailable failures in the HUD with retry guidance instead of silently doing nothing. Covered by a server-rendered HUD regression and browser denial probe/screenshot. Remaining for full task: sensitivity UX polish.
   - Slice done 2026-06-23: first-person now has deterministic mouse-look yaw/pitch math, clamped pitch, renderer camera pitch, and a HUD pointer-lock button. ESC while locked releases pointer lock and leaves first-person active; second ESC exits. Verified with regression test and browser probe/screenshot. Remaining for full task: polish sensitivity UX.
2. [~] Smooth WASD movement with acceleration/deceleration and better key handling.
   - Acceptance: held movement accelerates and release decelerates deterministically; diagonal input is normalized; repeated keydown does not stack speed; covered by control math tests and UI smoke.
   - Slice done 2026-06-23 14:24 SAST: first-person now clears held movement input whenever the operator switches to a different stepped-into citizen, so a stale held W/A/S/D key cannot make the next avatar drift before intentional input. Covered by a RED/GREEN dogfood regression and browser probe showing KOOKER stayed at `{x:229,y:367}` with `secondDeltaAfterSwitch: 0`; screenshot `/Users/joehermesbot/.hermes/cache/screenshots/browser_screenshot_a16116630bd6466498c0ad0a0c1bd29a.png`. Remaining for full task: optional keyboard-repeat browser event regression and subjective turn/strafe feel tuning.
   - Slice done 2026-06-23 13:25 SAST: WASD now behaves like FPS locomotion: W/S walk, A/D strafe, left/right arrows turn. Diagonal W+D strafing is normalized to the same travel distance as W alone and does not yaw the camera; HUD copy now explains `A/D strafe · arrows turn`. Covered by a RED/GREEN dogfood regression, full tests, typecheck, build, and browser probe/screenshot. Remaining for full task: optional key-repeat-specific regression and subjective turn/strafe feel tuning.
   - Slice done 2026-06-23: first-person walking now brakes immediately when forward and backward are held together, instead of coasting through conflicting input; covered by a dogfood regression and browser probe. Remaining for full task: any strafe-vs-turn decision and additional diagonal/turn-feel tuning if desired.
   - Slice done 2026-06-23: first-person walking now ramps toward configured max speed and briefly coasts down after release; tunables live in `COLONY.firstPerson`; covered by dogfood regression and browser smoke. Remaining for full task: explicit diagonal/key-repeat regression and any strafe-vs-turn decision.
   - Slice done 2026-06-23: runtime `setFpKey` now accepts browser `KeyboardEvent.code` names (`KeyW/A/S/D`) directly as well as normalized key names, so first-person dogfood and future browser controls share a less brittle input boundary. Remaining for full task: explicit diagonal/key-repeat regression and any strafe-vs-turn decision.
3. [~] Walkable collision/terrain guardrails: houses/shops/water/roads behavior.
   - Acceptance: first-person movement cannot settle in blocked buildings or water unless explicitly allowed; road/path cells feel preferred; blocked steps return a clear reason for HUD/narration; covered by route tests.
   - Slice done 2026-06-23: first-person movement now applies a configured road/path surface multiplier so roads feel easier to roam than off-road terrain; covered by a dogfood regression that compares equal-time road and off-road walks and a browser probe/screenshot. Remaining for full task: tune path-vs-road feel separately if needed and expand blocked reason taxonomy for special shop/interior affordances.
   - Slice done 2026-06-23: first-person movement now blocks reserved parcel/lot footprint cells that are not roads and reports `blockedReason: "parcel"`, while still letting avatars already spawned inside an occupied footprint walk out instead of getting trapped; covered by a dogfood regression and browser screenshot. Remaining for full task: road/path preference and richer blocked reason taxonomy.
   - Slice done 2026-06-23: first-person movement now blocks completed building cells and reports `blockedReason: "building"` to the HUD; covered by a dogfood regression and browser screenshot. Remaining for full task: shop/parcel footprint collision, road/path preference, and richer blocked reason taxonomy.
   - Slice done 2026-06-23: first-person movement now records a player-facing blocked reason for water/edge-of-map guardrails and the HUD shows `Blocked water`; covered by dogfood regression and browser screenshot. Remaining for full task: building/shop collision, road/path preference, and richer blocked reasons.
4. [x] First-person interaction affordance: nearest citizen/building/shop/action prompt.
   - Acceptance: first-person view exposes exactly one nearest useful interaction prompt from live avatar position, preferring nearby citizens then civic/buildings then roads; UI can show the prompt with rounded distances; covered by deterministic tests.
   - Done 2026-06-23: `FirstPersonView.interactionPrompt` now selects a single live-position prompt and the first-person panel renders it as an Action line. Verified with Vitest, typecheck, build, full test suite, and browser screenshot.
5. [x] Photo mode/demo capture: deterministic screenshot evidence and PR comment/update.
   - Acceptance: an operator can trigger a reproducible first-person demo screenshot without private data; automation captures the current citizen, prompt/HUD and position evidence; documented in the PR/tasklist.
   - Done 2026-06-23: `ColonyRuntime.captureFirstPersonDemo()` now returns a public-safe deterministic evidence payload (citizen, rounded live position/heading/pitch, action prompt, blocked reason, clock label, HUD lines) plus a browser PNG data URL when the renderer is mounted. Verified by RED/GREEN dogfood regression and browser probe; screenshot `/Users/joehermesbot/.hermes/cache/screenshots/browser_screenshot_7a7e655a573b435888cb4ff1127cb609.png`.
6. [x] Immersive HUD mode separating debug telemetry from player overlay.
   - Acceptance: player-facing first-person overlay shows only concise movement/interactions while debug telemetry remains behind an operator affordance; no raw private/backend data displayed; UI screenshot verifies.
   - Done 2026-06-23: First-person panel now defaults to a concise player HUD with action/blocked/urgent colony status, while ground/time/nearby debug telemetry stays behind a Show debug/Hide debug toggle. Verified with a server-rendered regression test, focused first-person tests, full Vitest, typecheck, build, and browser screenshots for both collapsed and expanded debug states.
7. [x] Route dogfood: scripted walk path with before/after position/camera assertions.
   - Acceptance: a test/script walks a deterministic path, asserts avatar position/heading/view changes after each step, and can be reused as browser dogfood.
   - Done 2026-06-23: Added `driveFirstPersonRouteDogfood`, a deterministic reusable route dogfood helper that enters first-person, drives the same key input path as the UI, samples before/after position and heading, and verifies the live first-person JSON view follows each step.
8. [x] First-person interaction execution beyond prompts.
   - Acceptance: the visible Action prompt is executable from keyboard and HUD controls; activation gives immediate player-facing feedback tied to the prompt kind without exposing private data; covered by deterministic runtime and server-rendered HUD regressions plus browser smoke.
   - Done 2026-06-23 14:45 SAST: added deterministic `activateFirstPersonInteraction()` feedback for the current Action prompt, wired the HUD `Use E` button plus keyboard `E`, and browser-smoked both paths. Evidence after activation showed `Action Talk to KOOKER the Builder`, narration `You talk to KOOKER the Builder.`, and screenshot `/Users/joehermesbot/.hermes/cache/screenshots/browser_screenshot_87400aa77225490b9524fe9bcaed5816.png`.
9. [x] Sprint modifier for first-person roaming.
   - Acceptance: holding Shift while walking gives a deterministic, config-backed speed boost without affecting normal walk, releases cleanly, and the HUD control hint tells players about Shift; covered by dogfood movement tests, HUD regression, and browser smoke.
   - Done 2026-06-23 15:06 SAST: added a config-backed Shift sprint modifier for first-person walking. Browser probe showed normal walk distance `1.19`, Shift sprint distance `1.7255`, ratio `1.45`, and the HUD displayed `Shift sprint`; screenshot `/Users/joehermesbot/.hermes/cache/screenshots/browser_screenshot_897d75bca7f148e5bf256991a759b15a.png`.
10. [x] Sprint comfort limiter for first-person roaming.

- Acceptance: sustained Shift sprint consumes a deterministic, config-backed stamina/comfort meter, exhausted sprint falls back to normal walking, releasing Shift recovers the meter, and the HUD shows the rounded sprint charge; covered by dogfood and HUD regressions plus browser smoke.
- Done 2026-06-23 15:29 SAST: sustained Shift sprint now spends a config-backed comfort charge, exhausted sprint falls back to normal walking, and releasing Shift recovers the meter even while standing still. The HUD shows the rounded `Sprint N%` charge. Browser probe returned `exhaustedCharge: 0`, `recoveredCharge: 75`, and screenshot `/Users/joehermesbot/.hermes/cache/screenshots/browser_screenshot_525d48a9635f49e3b93d8375e9af6230.png`.

11. [x] Visual sprint charge polish for first-person HUD.

- Acceptance: the first-person player HUD shows a compact visual sprint charge meter with accessible rounded percentage text, matching runtime sprint charge and staying free of debug/private telemetry; covered by server-rendered HUD regression plus browser smoke.
- Done 2026-06-23 15:48 SAST: added a compact accessible sprint charge progress meter under `Sprint N%`, and fixed the first-person browser input boundary so `ShiftLeft`/`ShiftRight` keydown actually reaches runtime sprint instead of only being cleared on keyup. Browser smoke depleted the meter to `Sprint 0%` with `aria-valuenow="0"` and width `0%`; screenshot `/Users/joehermesbot/.hermes/cache/screenshots/browser_screenshot_44abbb8819284b9b9d2112607d65949e.png`.

12. [x] Low-sprint feedback for first-person roaming.

- Acceptance: when sprint charge is low or depleted, the player HUD gives concise recovery guidance without exposing debug telemetry; covered by a server-rendered HUD regression plus browser smoke.
- Done 2026-06-23 16:01 SAST: added concise player-facing sprint recovery hints: `Sprint low — ease off to recover` below 20% and `Sprint depleted — walk to recover` at 0%. Browser smoke held real `ShiftLeft`+`KeyW` until the HUD showed `Sprint 0%`, the depleted hint, and an empty progress bar; screenshot `/Users/joehermesbot/.hermes/cache/screenshots/browser_screenshot_11587bce713a42578c84d49ab0997e6b.png`.

13. [x] Guided action side effects for first-person prompts.

- Acceptance: activating road/civic/building prompts does more than narrate: it sets a deterministic public-safe walking target or other immediate game-side effect, keeps citizen-talk safe, and exposes enough HUD/demo evidence to prove the effect.
- Done 2026-06-23 17:26 SAST: guided walks now actually advance the stepped-in avatar toward the public-safe target when the player is not manually steering, then clear the stale `Guided walk` target on arrival and show `Arrived at road.` in the HUD. Browser smoke verified distance to target improved from `1` to `0.7`, final position reached `{x:300,y:306}`, `guidedAfterArrival: null`, and screenshot `/Users/joehermesbot/.hermes/cache/screenshots/browser_screenshot_8d3947d830a84700918bc0a93e65fe12.png`.
- Slice done 2026-06-23 17:02 SAST: manual first-person movement now cancels an active guided walk immediately, clears the stale `Guided walk` HUD target, and shows `Guided walk canceled — manual control resumed.` so WASD takeover cannot leave obsolete route guidance. Browser smoke verified `guidedAfterActivate: {label: "road", x: 301, y: 306}`, then `guidedAfterManual: null`, no `Guided walk` DOM line, and screenshot `/Users/joehermesbot/.hermes/cache/screenshots/browser_screenshot_9b917cd492c84063b2083b849b331de8.png`. Remaining for full task: guided-target arrival polish when the player reaches the destination.
- Slice done 2026-06-23 16:41 SAST: civic and building prompts now share the guided-walk side effect with roads. Activating `Visit market` or `Inspect habitat` sets a public-safe rounded target, clears first-person walk speed, and shows `Guided walk <target> (x, y)` plus `Guiding you to <target>.`, while citizen prompts remain safe talk narration. Browser smoke verified civic target `{label: "market", x: 128, y: 128}` with screenshot `/Users/joehermesbot/.hermes/cache/screenshots/browser_screenshot_adaf44065ddf4610b56492979154d308.png`. Remaining for full task: guided-target cancellation/arrival polish when players manually take over or reach the destination.
- Slice done 2026-06-23 16:29 SAST: road prompts now become actionable guided walks. First-person prompts carry public-safe target coordinates, far-away citizens no longer hide nearby road prompts, activating `Follow road` sets a deterministic guided target, and the player HUD shows `Guided walk road (x, y)` plus `Guiding you to road.` Browser smoke verified target `(301, 306)` with screenshot `/Users/joehermesbot/.hermes/cache/screenshots/browser_screenshot_39efc5ba210241f69a77546b02048cf0.png`. Remaining for full task: add similarly useful side effects for civic/building prompts.

14. [x] Guided-walk demo capture evidence.

- Acceptance: deterministic first-person demo capture includes any active guided walk destination as rounded public-safe evidence and HUD lines, so PR/browser reports can prove interaction side effects without reading debug state; covered by dogfood capture regression plus browser smoke.
- Done 2026-06-23 17:41 SAST: `captureFirstPersonDemo()` now includes a rounded public-safe `guidedTarget` and matching `Guided walk <label> (x, y)` HUD evidence line whenever a guided walk is active. Browser smoke on port 5198 activated `Follow road` from Joe and capture evidence returned `guidedTarget: {label: "road", x: 300, y: 306}`, `hudLines: ["Follow road", "Guided walk road (300, 306)", "colony hungry"]`, and PNG data URL present; screenshot `/Users/joehermesbot/.hermes/cache/screenshots/browser_screenshot_59e861cceb6b4280bf6d80705c0d9663.png`. Next recommended slice: route/pathfinding autopilot polish beyond straight-line targeting, or collision taxonomy/interior affordances.

15. [x] Guided-walk distance feedback in first-person HUD and demo capture.

- Acceptance: while a guided walk is active, the player HUD and deterministic capture evidence show a rounded remaining distance to target; the value decreases during unattended guided movement and remains public-safe; covered by dogfood/capture and server-rendered HUD regressions plus browser smoke.
- Done 2026-06-23 18:07 SAST: active guided walks now expose rounded `remainingDistance` in first-person UI state and deterministic demo capture evidence. The player HUD shows `Guided walk road (300, 306) · 1 unit away`, and browser evidence proved unattended movement reduced the capture distance from `1` to `0.7` before arrival. Screenshot `/Users/joehermesbot/.hermes/cache/screenshots/browser_screenshot_349c6bda405147a3952d86df565fce18.png`. Next recommended slice: route/pathfinding autopilot polish beyond straight-line targeting, or collision taxonomy/interior affordances.

16. [x] Guided-walk collision guardrails.

- Acceptance: unattended guided walks use the same first-person walkability guardrails as manual WASD, do not pass through blocked terrain/parcel/building cells, and surface a player-facing blocked reason while preserving the guided target for retry/manual takeover; covered by deterministic dogfood regression plus browser smoke.
- Done 2026-06-23 18:27 SAST: guided autopilot now steps the first-person avatar through the same `blockedStepReason` guardrail used by manual WASD instead of handing it to straight-line roster walking. If a reserved parcel/blocked cell is ahead, the avatar stays put, the guided target remains visible, `blockedReason` becomes `parcel`, and HUD narration says `Guided walk blocked by parcel.` Browser smoke on port 5200 showed start `{x:298,y:306}`, blocker `{x:299,y:306}`, target `{x:300,y:306}`, retained guided target, and screenshot `/Users/joehermesbot/.hermes/cache/screenshots/browser_screenshot_e6b9acfe5fba490686f7f3db1afbbdda.png`. Next recommended slice: true route/pathfinding autopilot around blockers, or expand the guided guardrail regression table to building/water cases.

17. [x] Guided-walk manual recovery polish.

- Acceptance: after an unattended guided walk is blocked, any deliberate manual takeover cancels stale route guidance and stale blocked HUD state immediately, even for turn-only input before a successful move; covered by deterministic dogfood regression plus browser smoke.
- Done 2026-06-23 18:44 SAST: clearing a blocked guided walk with manual input now also clears stale `blockedReason`, so the HUD shows `Guided walk canceled — manual control resumed.` without lingering `Blocked parcel`. Browser smoke on port 5201 reproduced a guided road target `{x:300,y:306}` blocked by parcel `{x:299,y:306}`, then sent `ArrowRight`; recovered state was `guidedTarget: null`, `blockedReason: null`, heading `0.24`, and screenshot `/Users/joehermesbot/.hermes/cache/screenshots/browser_screenshot_52ec74abb80b48e199f6c965a1912aae.png`. Next recommended slice: true route/pathfinding autopilot around blockers, or expand guided guardrail coverage to building/water cases.

18. [x] Guided-walk collision sweep guardrail.

- Acceptance: unattended first-person guided walks must not tunnel through a blocked parcel/building/water cell when automation advances a large deterministic step; path checking samples the segment with the same walkability guardrails and preserves player-facing blocked feedback. Covered by RED/GREEN dogfood regression plus browser smoke.
- Done 2026-06-23 19:07 SAST: guided autopilot now sweeps large deterministic movement segments with config-backed collision samples before moving, so a 3-second dogfood step cannot jump over the parcel cell between Joe and a road target. Browser smoke on port 5202 reproduced start `{x:298,y:306}`, blocker `{x:299,y:306}`, target `{x:300,y:306}`, retained guided target, `blockedReason: "parcel"`, narration `Guided walk blocked by parcel.`, and screenshot `/Users/joehermesbot/.hermes/cache/screenshots/browser_screenshot_4aef1f6a1d0b44d694f81d5fa5b02ae3.png`. Next recommended slice: true route/pathfinding autopilot around blockers, or expand the collision sweep regression to building/water cases.

19. [x] Mobile first-person roaming dock polish.

- Acceptance: on narrow/mobile viewports, the first-person player controls remain reachable without the side HUD covering the view; touch buttons have accessible labels and larger tap targets; covered by server-rendered/UI class regressions plus a mobile browser walkthrough video or honest fallback.
- Done 2026-06-23 19:25 SAST: first-person touch controls now have mobile class hooks, accessible walk labels, larger phone tap targets, and the hint copy `Tap arrows to roam`; the right-side city HUD receives `hud--first-person-mobile-drawer` while first-person is active and slides/fades right on phone widths. Verified with focused Vitest, full Vitest, typecheck, production build, and phone-width browser video evidence: `/Users/joehermesbot/.hermes/cache/citylife-fps-demo/20260623-1922/mobile-first-person-dock.mp4`.

20. [x] Guided-walk reroute around simple blockers.

- Acceptance: when a guided first-person road walk has a one-cell parcel blocker but a clear adjacent detour, the avatar routes around the blocker instead of stopping or tunneling through it; if the start is enclosed, existing blocked feedback and manual recovery still work; covered by deterministic dogfood regression plus desktop/mobile video evidence.
- Done 2026-06-23 19:52 SAST: guided autopilot now asks the deterministic terrain router for a next waypoint when its direct segment is blocked, while still using the first-person segment sweep before every movement. Browser evidence used start `{x:298,y:306}`, parcel blocker `{x:299,y:306}`, detour via `y:307`, and target `{x:300,y:306}`; desktop/mobile demos ended with `guidedTarget: null`, `blockedReason: null`, and `Arrived at road.`. Videos: `/Users/joehermesbot/.hermes/cache/citylife-fps-demo/20260623-194608/desktop-guided-reroute.mp4`, `/Users/joehermesbot/.hermes/cache/citylife-fps-demo/20260623-194608/mobile-guided-reroute.mp4`. Next recommended slice: expose route progress/waypoints in the HUD, or expand reroute coverage to water/building blockers.

21. [x] Guided-walk route progress HUD.

- Acceptance: when first-person guided routing detours around a blocker, public UI/demo evidence includes a rounded next waypoint so players can see the current route leg; the HUD stays concise/mobile-safe and no private/debug telemetry leaks. Covered by dogfood capture and server-rendered HUD regressions plus desktop/mobile demo evidence.
- Done 2026-06-23 20:11 SAST: guided detours now expose public-safe `nextWaypoint` coordinates on `guidedTarget`, include `Next leg (x, y)` in deterministic demo capture HUD lines, and render a concise `Next leg` row in the first-person HUD without showing debug telemetry. Desktop browser evidence showed `Guided walk road (300, 306) · 2 units away` plus `Next leg (298, 307)`; screenshot `/Users/joehermesbot/.hermes/cache/screenshots/browser_screenshot_7930060798fa4cb19017e82a5a12c3ab.png`. Mobile/video attempt via headless Chrome CDP produced a valid 390px structured runtime probe with `nextWaypoint: {x: 298, y: 307}` but blank screenshots/no rendered touch controls, so no MP4 is attached for this slice. Next recommended slice: fix headless/mobile capture reliability or expand reroute coverage to water/building blockers.

22. [~] Guided-walk reroute coverage for water/building/civic blockers.

- Acceptance: guided first-person walks route around simple water/building/civic blockers using the same deterministic router and segment sweep as parcel blockers, while enclosed/no-route cases still surface a player-facing blocked reason and preserve the target. Covered by RED/GREEN dogfood regressions plus desktop/mobile demo evidence.
- Slice done 2026-06-23 20:49 SAST: building prompts now guide to a reachable adjacent approach cell when the building footprint itself is blocked, so `Inspect habitat` does not try to walk Joe into the occupied building cell. Browser evidence used building `{x:253,y:98}`, start `{x:251,y:98}`, approach `{x:252,y:98}` and arrived with `blockedReason: null`; screenshots `/Users/joehermesbot/.hermes/cache/screenshots/browser_screenshot_b328d52c84754f7fa01aa1165cb43f37.png` and `/Users/joehermesbot/.hermes/cache/screenshots/browser_screenshot_c8027a6f497b40e99ed31146ad7867ad.png`. Video was attempted through installed Chrome/CDP at desktop and 390px mobile viewports, but the captured frames were blank even though the runtime probe succeeded, so no video is treated as valid evidence for this slice.
- Slice done 2026-06-23 21:09 SAST: civic prompts now guide to a reachable adjacent approach cell when the civic footprint itself is blocked, so `Visit market` does not try to walk Joe into the occupied market cell. Browser evidence used civic `{x:253,y:98}`, start `{x:251,y:98}`, approach `{x:252,y:98}` and arrived with `blockedReason: null`; desktop screenshot `/Users/joehermesbot/.hermes/cache/screenshots/browser_screenshot_3965be0806d24a5098043b1599e7619b.png`; mobile MP4 `/Users/joehermesbot/.hermes/cache/citylife-fps-demo/20260623-210558-gui-video/civic-approach-mobile.mp4`. Remaining for full task: water-specific reroute/approach coverage and broader no-route cases.

23. [x] End-of-day first-person demo/status pass.

- Acceptance: final window verifies the accumulated first-person branch with focused tests, full Vitest, typecheck, build, a fresh desktop and mobile demo attempt, PR status/checks, and a merge/hold recommendation without merging.
- Done 2026-06-23 22:10 SAST: final pass discovered and fixed a mobile touch-control regression: compass buttons called `walkStep()`, but that only set a roster target that the first-person driver immediately pinned back to the current position, so phone taps could show reachable controls without moving the avatar. Added a RED/GREEN dogfood regression for the touch-compass public path and changed `walkStep()` to move the active first-person avatar through the same segment collision guardrails, clear stale guided/blocked state, and narrate from the new position. Verification passed focused first-person/mobile tests, full Vitest, typecheck, and build. Fresh Playwright/installed-Chrome videos captured desktop keyboard roaming and mobile touch-button movement: `/Users/joehermesbot/.hermes/cache/citylife-fps-demo/20260623-2201-final/desktop-final-first-person.mp4`, `/Users/joehermesbot/.hermes/cache/citylife-fps-demo/20260623-2201-final/mobile-final-first-person.mp4`. PR #69's original GitHub object is merged/closed; the follow-up branch is now tracked by open PR #84, so Jojo should review/merge PR #84 rather than the historical PR #69.

## Run log

- 2026-06-23 22:10 SAST: Completed task 23 final first-person demo/status pass and fixed the touch-control blocker found during mobile evidence capture. Root cause: `FirstPersonPanel`'s compass buttons use `runtime.walkStep(dx, dy)`, but `walkStep()` only called `citizens.setTarget()`. While first-person is active, the RAF/dogfood driver pins the roster target back to the live avatar position unless guided/manual key input is active, so phone taps could leave Joe visually stationary despite accessible controls. Added a RED regression `moves the active first-person avatar from the touch compass walk controls`, watched it fail with `{x:251,y:98}` staying unchanged instead of `{x:251,y:99}`, then fixed `walkStep()` to move the active first-person avatar directly through `blockedSegmentReason()`, reset stale guided/blocked transient state, and narrate from the new position. Verification: focused RED/GREEN test passed; focused `tests/firstPersonDogfood.test.ts tests/firstPersonPanel.test.ts tests/mobileFirstPersonHud.test.ts` passed 3 files / 37 tests; full Vitest passed 65 files / 894 tests; `npm run typecheck` passed; `npm run build` passed with only the existing Vite chunk-size warning. Browser evidence on port 5210 used installed Chrome/Playwright video recording and ffmpeg conversion: desktop keyboard path moved Joe 3.976 units, mobile touch path moved Joe 8.606 units, mobile had `hud--first-person-mobile-drawer`, `Tap arrows to roam`, 9 touch buttons, and the north button measured 81x56. Videos: `/Users/joehermesbot/.hermes/cache/citylife-fps-demo/20260623-2201-final/desktop-final-first-person.mp4`, `/Users/joehermesbot/.hermes/cache/citylife-fps-demo/20260623-2201-final/mobile-final-first-person.mp4`. Screenshots: `/Users/joehermesbot/.hermes/cache/citylife-fps-demo/20260623-2201-final/desktop-final-first-person.png`, `/Users/joehermesbot/.hermes/cache/citylife-fps-demo/20260623-2201-final/mobile-final-first-person.png`. GitHub status: PR #69 is already merged/closed; follow-up branch `joe/first-person-live-position` is tracked by open PR #84, so push there and let Jojo review/merge PR #84. Next recommended slice: water-specific guided reroute/approach coverage, plus a small mobile drawer polish pass if Irwin wants the right-side peek tab less visually present.

- 2026-06-23 21:09 SAST: Advanced task 22 civic prompt approach targets. Root cause: first-person civic prompts, like `Visit market`, are backed by completed building/civic footprint cells that `blockedStepReason()` intentionally treats as occupied, but `activateFirstPersonInteraction()` only computed adjacent approach cells for prompts classified as `building`. That meant civic guided walks could target the blocked footprint and rely on later collision blocking instead of guiding to a usable approach. Added a RED dogfood regression for a synthetic market at `{x:253,y:98}` with Joe starting two cells away; it failed because the guided target was the blocked market footprint `{x:253,y:98}` instead of the reachable approach `{x:252,y:98}`. The fix applies the existing deterministic approach-target selection to any road/civic/building prompt whose raw target is blocked, while preserving unblocked direct targets. Verification: focused RED failed as expected, focused GREEN passed, full `tests/firstPersonDogfood.test.ts` passed 29 tests, full Vitest passed 62 files / 883 tests, `npm run typecheck` passed, and `npm run build` passed with only the existing Vite chunk-size warning. Browser desktop evidence on port 5206 returned prompt `Visit market`, guided target `{label:"market",x:252,y:98}`, final position `{x:252,y:98}`, `blockedReason:null`, and `Arrived at market.`; mobile 390px evidence showed `hud--first-person-mobile-drawer`, `Tap arrows to roam`, and `Arrived at market.` in `/Users/joehermesbot/.hermes/cache/citylife-fps-demo/20260623-210558-gui-video/civic-approach-mobile.mp4`. Next recommended slice: water-specific guided reroute/approach coverage, or broader no-route cases.

- 2026-06-23 20:49 SAST: Advanced task 22 building prompt approach targets. Root cause: first-person building prompts expose the building footprint as `targetXY`, but completed building cells are intentionally blocked by `blockedStepReason()`. Activating `Inspect habitat` therefore could set a guided target inside an occupied/blocked footprint and rely on later collision handling instead of guiding to a usable approach point. Added a RED dogfood regression for a synthetic habitat at `{x:253,y:98}` with Joe starting two cells away; it expects activation to choose the reachable adjacent `{x:252,y:98}` approach, not the blocked building cell, then arrive without blocked feedback. The fix computes a deterministic adjacent approach target for blocked building prompt destinations using the same `blockedStepReason()` and `leastCostPath()` guardrails. Verification: focused building test passed, full `tests/firstPersonDogfood.test.ts` passed 28 tests, full Vitest passed 62 files / 882 tests, `npm run typecheck` passed, and `npm run build` passed with only the existing Vite chunk-size warning. Browser desktop screenshots captured the prompt before activation and `Arrived at habitat.` after stepping. Installed Chrome/CDP video attempts produced structured runtime probes at desktop and 390px mobile viewports, but the frames were blank/no DOM controls, so the run reports that blocker honestly. Next recommended slice: add water-specific guided reroute/approach coverage, or fix the headless/mobile capture reliability.

- 2026-06-23 20:11 SAST: Completed task 21 guided-walk route progress HUD. Root cause: after guided rerouting landed, the public HUD and capture evidence still showed only the final target and remaining distance; players/reviewers could not see which detour leg the autopilot was currently taking around a blocker. Added RED dogfood/capture coverage that failed with missing `nextWaypoint`, plus a RED server-rendered HUD regression for `Next leg`. The fix computes a rounded next waypoint from the same deterministic `firstPersonGuidedWaypoint()` route helper when the direct guided segment is blocked, adds it to public-safe `guidedTarget` state/capture evidence, and renders `Next leg (x, y)` in the concise player overlay. Verification: focused RED failed as expected; focused GREEN passed; full Vitest passed 62 files / 881 tests; `npm run typecheck` passed after the final formatting patch; `npm run build` passed with only the existing Vite chunk-size warning. Browser desktop evidence on port 5205 returned start `{x:298,y:306}`, blocker `{x:299,y:306}`, target `{x:300,y:306}`, `nextWaypoint: {x:298,y:307}`, HUD lines including `Next leg (298, 307)`, and screenshot `/Users/joehermesbot/.hermes/cache/screenshots/browser_screenshot_7930060798fa4cb19017e82a5a12c3ab.png`. Video/mobile evidence was attempted through installed Chrome/CDP because Playwright is not installed; the CDP runtime probe at 390x844 succeeded but screenshots were blank/no DOM controls, so this run reports the capture blocker honestly. Next recommended slice: fix headless/mobile capture reliability or expand reroute coverage to water/building blockers.

- 2026-06-23 19:52 SAST: Completed task 20 guided-walk reroute around simple blockers. Root cause: guided walking had collision sampling to prevent tunneling, but when a single blocked parcel sat between the avatar and the road target it could only stop with `Guided walk blocked by parcel.` even if a clean adjacent detour existed. Added a RED dogfood regression for start `{x:298,y:306}`, blocker `{x:299,y:306}`, target `{x:300,y:306}`, and clear detour cells at `y:307`; it initially failed with `blockedReason: "parcel"`. The fix computes a deterministic least-cost waypoint when the direct guided segment is blocked, keeps the existing segment sweep for every waypoint move, and preserves enclosed-start blocked/manual-recovery regressions by surrounding those legacy fixtures with blockers. Verification: focused RED failed as expected; focused GREEN passed; `tests/firstPersonDogfood.test.ts` passed 27 tests; full Vitest passed 62 files / 880 tests; `npm run typecheck` passed; `npm run build` passed with only the existing Vite chunk-size warning. Browser evidence on port 5204 recorded desktop and mobile MP4s; final structured states were `guidedTarget: null`, `blockedReason: null`, `narration: "Arrived at road."`. Next recommended slice: expose route progress/waypoints in the HUD, or expand reroute coverage to water/building blockers.

- 2026-06-23 19:33 SAST: Completed task 19 mobile first-person roaming dock polish. Root cause: on phone-width viewports the normal side HUD competes with the first-person touch controls and can cover too much of the roaming view, while the compass buttons had compact desktop sizing and terse labels. The slice already had RED/GREEN server-rendered regressions and UI class coverage in the working tree; I verified the branch state, then captured the required mobile walkthrough video. Implementation gives `FirstPersonPanel` stable mobile class hooks, accessible `Walk north/east/...` labels, larger touch targets under the phone media query, the player hint `Tap arrows to roam`, and `hudClassName()` so the side HUD becomes a sliding/fading mobile drawer only while first-person is active. Verification: focused `npm test -- --run tests/firstPersonPanel.test.ts tests/mobileFirstPersonHud.test.ts` passed 2 files / 6 tests; full `npm test -- --run` passed 62 files / 879 tests; `npm run typecheck` passed; `npm run build` passed with only the existing Vite chunk-size warning. Browser evidence on port 5203 used a 390x844 viewport and returned panel class `first-person-panel`, HUD class `hud hud--first-person-mobile-drawer`, 9 touch buttons each 81x56 with accessible labels, plus MP4 `/Users/joehermesbot/.hermes/cache/citylife-fps-demo/20260623-1922/mobile-first-person-dock.mp4` and screenshot `/Users/joehermesbot/.hermes/cache/citylife-fps-demo/20260623-1922/mobile-first-person-dock.png`. Next recommended slice: true route/pathfinding autopilot around blockers, or expand guided collision sweep regression coverage to building/water cases.

- 2026-06-23 19:07 SAST: Completed task 18 guided-walk collision sweep guardrail. Root cause: `driveFirstPersonGuided()` checked only the final `(nx, ny)` of each deterministic dogfood/RAF step with `blockedStepReason()`. A large automation step could therefore move Joe from `{x:298,y:306}` directly to the road target `{x:300,y:306}` while skipping the blocked parcel cell rounded at `{x:299,y:306}`. Added a RED dogfood regression for a 3-second guided step with a parcel blocker between start and target; it failed with `blockedReason: null`. The fix adds config-backed `guidedCollisionSampleStep` and sweeps guided movement segments before mutating position, preserving `guidedTarget` and `Guided walk blocked by parcel.` when any sampled point is blocked. Verification: focused RED failed as expected, focused GREEN passed, `tests/firstPersonDogfood.test.ts` passed 26 tests, full Vitest passed 61 files / 877 tests, typecheck passed, build passed. Browser smoke on port 5202 returned retained target `{label: "road", x: 300, y: 306}`, `blockedReason: "parcel"`, narration `Guided walk blocked by parcel.`, no console errors, and screenshot `/Users/joehermesbot/.hermes/cache/screenshots/browser_screenshot_4aef1f6a1d0b44d694f81d5fa5b02ae3.png`. Next recommended slice: true route/pathfinding autopilot around blockers, or expand collision sweep regression coverage to building/water cases.

- 2026-06-23 18:44 SAST: Completed task 17 guided-walk manual recovery polish. Root cause: guided autopilot correctly preserved a blocked guided target and surfaced `blockedReason: "parcel"`, but the manual takeover branch only cleared `fpGuidedTarget`; turn-only recovery canceled the route while leaving stale `Blocked parcel` HUD feedback until a later successful move. Added a RED dogfood regression for a blocked guided walk followed by `ArrowRight` manual takeover; it initially failed with `blockedReason: "parcel"`. The fix clears `fpBlockedReason` at the same first-person manual-control boundary that cancels guided walks. Verification: focused RED failed as expected, focused GREEN passed, `tests/firstPersonDogfood.test.ts` passed 25 tests, full Vitest passed 61 files / 876 tests, typecheck passed, build passed. Browser smoke on port 5201 returned blocked state `parcel`, then recovered state `guidedTarget: null`, `blockedReason: null`, narration `Guided walk canceled — manual control resumed.`, no console errors, and screenshot `/Users/joehermesbot/.hermes/cache/screenshots/browser_screenshot_52ec74abb80b48e199f6c965a1912aae.png`. Next recommended slice: true route/pathfinding autopilot around blockers, or expand guided guardrail coverage to building/water cases.

- 2026-06-23 18:27 SAST: Completed task 16 guided-walk collision guardrails. Root cause: after guided walks gained unattended motion, the first-person avatar was still moved by `CitizenRoster.stepAvatars()`, whose straight-line easing does not consult first-person collision/walkability checks. That meant a guided walk could pass through cells that manual WASD already blocks. Added a RED dogfood regression with a road prompt two cells away and a reserved parcel cell in between; it initially failed with `blockedReason: null`. Implemented a first-person guided stepper in `driveFirstPerson()` that uses `blockedStepReason()`, pins the roster target to the current position so the generic straight-line walker cannot bypass the guardrail, preserves the guided target, and surfaces `Guided walk blocked by parcel.` Verification: focused RED/GREEN passed, full first-person dogfood passed 24 tests, full Vitest passed 61 files / 875 tests, typecheck passed, build passed. Browser smoke on port 5200 returned blocked reason `parcel`, narration `Guided walk blocked by parcel.`, target `{label: "road", x: 300, y: 306}`, and screenshot `/Users/joehermesbot/.hermes/cache/screenshots/browser_screenshot_e6b9acfe5fba490686f7f3db1afbbdda.png`. Next recommended slice: true route/pathfinding autopilot around blockers, or expand guided guardrail coverage to building/water cases.

- 2026-06-23 18:07 SAST: Completed task 15 guided-walk distance feedback. Root cause: after guided walks landed, the HUD and photo/demo evidence showed only a target coordinate, so reviewers could not see live progress without inspecting avatar positions or waiting for arrival. Added RED dogfood/capture regressions for `remainingDistance` on guided targets, RED HUD coverage for player-facing `N unit(s) away` copy, then computed rounded public-safe distance from the stepped-in avatar to the guided target in `getUiState()` and reused it in `captureFirstPersonDemo()`. Verification: initial RED failed with missing/undefined `remainingDistance` and absent HUD copy; focused Vitest passed 27 tests; full Vitest passed 61 files / 874 tests; typecheck passed; build passed. Browser smoke on port 5199 returned capture evidence before/after guided movement `1 -> 0.7`, HUD lines `Guided walk road (300, 306) · 1 unit away` and then `0.7 units away`, no console errors, and screenshot `/Users/joehermesbot/.hermes/cache/screenshots/browser_screenshot_349c6bda405147a3952d86df565fce18.png`. Next recommended slice: route/pathfinding autopilot polish beyond straight-line targeting, or collision taxonomy/interior affordances.

- 2026-06-23 17:41 SAST: Completed task 14 guided-walk demo capture evidence. Root cause: `captureFirstPersonDemo()` exported action/blocked/clock HUD evidence but not the active guided target set by road/civic/building interactions, so PR/browser evidence had to read debug UI state to prove a guided-walk side effect. Added a RED dogfood capture regression that failed with `guidedTarget: undefined`, then added rounded public-safe `guidedTarget` evidence and a matching `Guided walk <label> (x, y)` HUD line. Verification: RED failed as expected, focused test passed, `tests/firstPersonDogfood.test.ts` passed 23 tests, `tests/firstPersonPanel.test.ts` passed 4 tests, full Vitest passed 61 files / 874 tests, typecheck passed, build passed. Browser smoke on port 5198 returned capture evidence `guidedTarget: {label: "road", x: 300, y: 306}`, `hudLines: ["Follow road", "Guided walk road (300, 306)", "colony hungry"]`, PNG data URL present, and screenshot `/Users/joehermesbot/.hermes/cache/screenshots/browser_screenshot_59e861cceb6b4280bf6d80705c0d9663.png`. Next recommended slice: route/pathfinding autopilot polish beyond straight-line targeting, or collision taxonomy/interior affordances.

- 2026-06-23 17:26 SAST: Completed task 13 guided action side effects. Root cause: activating road/civic/building prompts called `setTarget`, but the first-person driver immediately pinned the stepped-in avatar target back to the current position every frame, so an unattended guided walk never advanced and could not produce arrival feedback. Added a RED dogfood regression that first proved a guided road target stayed one unit away, then changed the driver to leave guided targets intact while no manual input is held, made the dogfood hook mirror the RAF loop by stepping avatars, and added a config-backed arrival distance that clears `guidedTarget` with `Arrived at <label>.` Verification: focused RED/GREEN test passed, focused first-person file passed 22 tests, full Vitest passed 61 files / 873 tests, typecheck passed, build passed. Browser smoke on port 5197 returned distance `1 -> 0.7 -> 0`, final position `{x: 300, y: 306}`, `guidedAfterArrival: null`, narration `Arrived at road.`, and screenshot `/Users/joehermesbot/.hermes/cache/screenshots/browser_screenshot_8d3947d830a84700918bc0a93e65fe12.png`. Next recommended slice: create a fresh task for route/pathfinding autopilot polish beyond straight-line avatar targeting, or return to collision taxonomy/interior affordances.

- 2026-06-23 16:41 SAST: Advanced task 13 guided action side effects. Root cause: first-person action execution only converted road prompts into a real guided target; nearby civic/building prompts still produced narration-only feedback despite already carrying public-safe `targetXY` coordinates. Added RED dogfood coverage for synthetic `market` civic and `habitat` building prompts, watched both fail with `guidedTarget: null`, then generalized the existing guided-walk activation branch to road/civic/building while preserving citizen talk narration. Verification: focused `tests/firstPersonDogfood.test.ts` passed 20 tests after the RED failure, full Vitest passed 61 files / 871 tests, typecheck passed, build passed. Browser smoke on port 5196 returned prompt `{kind: "civic", label: "Visit market", targetXY: {x: 128, y: 128}}`, guided target `{label: "market", x: 128, y: 128}`, narration `Guiding you to market.`, no console errors, and screenshot `/Users/joehermesbot/.hermes/cache/screenshots/browser_screenshot_adaf44065ddf4610b56492979154d308.png`. Next recommended slice: guided-target cancellation/arrival polish when players manually take over or reach the destination.

- 2026-06-23 16:29 SAST: Advanced task 13 guided action side effects. Root cause: first-person action execution could narrate `Follow road`, but it did not set a game-side destination, and the prompt chooser treated any citizen anywhere in the roster as interactable, which could hide the nearby road action. Added RED dogfood coverage for a road prompt away from citizens and RED server-rendered HUD coverage for guided target feedback. Implemented config-backed prompt distance thresholds, public-safe `targetXY` on prompts, road activation that sets a guided target, and a concise `Guided walk road (x, y)` HUD line. Verification: focused Vitest passed 22 tests, full Vitest passed 61 files / 869 tests, typecheck passed, build passed. Browser smoke on port 5195 returned prompt `{kind: "road", targetXY: {x: 301, y: 306}}`, guided target `{label: "road", x: 301, y: 306}`, narration `Guiding you to road.`, no console errors, and screenshot `/Users/joehermesbot/.hermes/cache/screenshots/browser_screenshot_39efc5ba210241f69a77546b02048cf0.png`. Next recommended slice: civic/building prompt side effects such as guided walking to a civic/building target with specific arrival feedback, or clear guided target cancellation when the player takes manual control.

- 2026-06-23 16:01 SAST: Completed task 12 low-sprint feedback. Root cause: the sprint meter visually depleted, but at low/empty charge the player still had to infer why sprint stopped and how to recover. Added RED server-rendered HUD regressions for depleted and low-charge guidance, then rendered compact recovery copy inside the player overlay without exposing debug telemetry. Verification: focused panel test passed, full Vitest passed 61 files / 867 tests, typecheck passed, build passed. Browser smoke on port 5194 drove real `ShiftLeft`+`KeyW` until charge reached `0`, with DOM evidence `aria-valuenow: "0"`, panel text `Sprint 0%` plus `Sprint depleted — walk to recover`, and no JS errors; screenshot `/Users/joehermesbot/.hermes/cache/screenshots/browser_screenshot_11587bce713a42578c84d49ab0997e6b.png`. Next recommended slice: guided-walk side effect for road/civic action prompts so interactions start doing more than narrating.

- 2026-06-23 15:48 SAST: Completed task 11 visual sprint charge polish. Root cause: the sprint comfort slice exposed only text, so players could not read sprint budget at a glance; browser probing also showed `ShiftLeft` keydown was not in the first-person keydown set even though keyup cleared it, meaning real browser Shift sprint depended on direct runtime probes rather than the UI input boundary. Added RED server-rendered HUD coverage for an accessible `progressbar` with rounded charge, plus a RED input-boundary regression for Shift sprint keys. The HUD now renders a compact fill bar under `Sprint N%`, and first-person keyboard handling uses a shared key-code helper that includes ShiftLeft/ShiftRight. Verification: focused tests passed, full Vitest passed 61 files / 865 tests, typecheck passed, build passed. Browser smoke on port 5193 drove real `KeyboardEvent` Shift+W, depleting the HUD to `Sprint 0%`, `aria-valuenow: "0"`, width `0%`; screenshot `/Users/joehermesbot/.hermes/cache/screenshots/browser_screenshot_44abbb8819284b9b9d2112607d65949e.png`. Next recommended slice: guided-walk side effect for road/civic action prompts, or richer low-stamina feedback if Irwin wants more movement feel polish.

- 2026-06-23 15:29 SAST: Completed task 10 sprint comfort limiter. Root cause: the new Shift sprint made long first-person roaming feel better, but without a recovery budget sprint could become the default always-held movement mode. Added RED dogfood coverage for sprint exhaustion, normal-speed fallback, release recovery while stopped, and a RED HUD regression for `Sprint N%`; then added config-backed `sprintChargeSeconds`/`sprintRecoverySeconds`, runtime sprint charge state, and player-facing HUD charge text. Verification: focused first-person tests passed, full Vitest passed 60 files / 864 tests, typecheck passed, build passed. Browser smoke on port 5192 showed exhausted charge `0`, recovered charge `75`, then HUD `Sprint 100%`; screenshot `/Users/joehermesbot/.hermes/cache/screenshots/browser_screenshot_525d48a9635f49e3b93d8375e9af6230.png`. Next recommended slice: guided-walk side effect for road/civic action prompts, or a more visual stamina bar/pulse polish if Irwin wants movement feel first.

- 2026-06-23 15:06 SAST: Completed task 9 Shift sprint roaming. Root cause: long first-person road walks still used the same carefully tuned normal walking speed, so the operator had no FPS-style way to cover distance faster while preserving the slower roam feel. Added RED dogfood and HUD regressions, then mapped `ShiftLeft`/`ShiftRight` to a transient sprint input, applied a `COLONY.firstPerson.sprintWalkSpeedMultiplier`, and updated the HUD hint. Browser smoke on port 5191 returned walk distance `1.19`, sprint distance `1.7255`, ratio `1.45`, with no console errors; screenshot `/Users/joehermesbot/.hermes/cache/screenshots/browser_screenshot_897d75bca7f148e5bf256991a759b15a.png`. Next recommended slice: add a lightweight stamina/comfort limiter or guided-walk interaction side effect so sprint remains useful without becoming the only movement mode.

- 2026-06-23 14:45 SAST: Completed task 8 first-person action execution. Root cause: the first-person HUD could surface a nearest `Action` prompt but there was no deterministic way to activate it; operators could only narrate or move. Added RED runtime and server-rendered HUD regressions, then implemented `activateFirstPersonInteraction()` with public-safe kind-specific feedback, wired the `Use E` HUD button, and mapped keyboard `E` through the first-person input boundary. Browser smoke verified the button and a synthetic `KeyE` event both produced `You talk to KOOKER the Builder.` with no console errors; screenshot `/Users/joehermesbot/.hermes/cache/screenshots/browser_screenshot_87400aa77225490b9524fe9bcaed5816.png`. Next recommended slice: evolve interaction execution into real side effects for specific prompts, e.g. citizen talk opens/socially pings KookerBook or road/civic prompts set a short guided walking target.

- 2026-06-23 14:24 SAST: Advanced task 2 stale held-key switching guardrail. Root cause: `enterFirstPerson()` reset speed, pitch and blocked reason but not the held-key set, so switching from Joe to KOOKER while `KeyW` was still logically down made KOOKER drift `0.9` world units on the next first-person step. Added a RED dogfood regression for citizen switching with a stale held W key, then fixed the source by clearing `fpKeys` on every first-person entry/switch. Browser probe after the fix returned `secondBefore: {x:229,y:367}`, `secondAfter: {x:229,y:367}`, `secondDeltaAfterSwitch: 0`, `heldKeys: []`; screenshot `/Users/joehermesbot/.hermes/cache/screenshots/browser_screenshot_a16116630bd6466498c0ad0a0c1bd29a.png`. Next recommended slice: optional keyboard-repeat browser event regression or first-person interaction execution beyond prompts.
- 2026-06-23 14:01 SAST: Completed task 1 mouse-look sensitivity polish. Root cause: pointer-lock/mouse-look had a fixed config sensitivity, so operators with different devices could only recover by careful movement or code changes. Added a RED dogfood regression proving Low/Normal/High presets scale both yaw and pitch, and a RED server-rendered HUD regression for the visible `Look sensitivity` controls. Runtime now stores `firstPerson.mouseSensitivity`, applies config-backed preset scales, and the HUD exposes Low/Normal/High buttons with the selected preset. Browser probe selected High and returned `mouseSensitivity: "high"`, `yawDelta: 0.16`, `lookPitch: 0.16`; screenshot `/Users/joehermesbot/.hermes/cache/screenshots/browser_screenshot_90f8e13b97104bf8982db963afbd2193.png`. Next recommended slice: task 2 key-repeat-specific regression, or start a new task for first-person interaction execution beyond prompts.
- 2026-06-23 13:45 SAST: Advanced task 1 mouse-look comfort polish. Root cause: after pointer-lock/mouse-look landed, there was no visible recovery affordance for an over-tilted pitch; the only way back to level was careful mouse movement or exiting first-person. Added a RED runtime regression for `levelFirstPersonLook()` preserving yaw while resetting pitch to `0`, plus a RED server-rendered HUD regression for the `Level view` control. Browser probe after synthetic mouse-look returned heading `1.7208`, pitch `0.4`, then after clicking Level view returned the same heading and pitch `0`; screenshot `/Users/joehermesbot/.hermes/cache/screenshots/browser_screenshot_561656388d874ef48c482238a19c71eb.png`. Next recommended slice: task 1 optional mouse sensitivity preference/slider, or task 2 key-repeat-specific regression.
- 2026-06-23 13:25 SAST: Advanced task 2 diagonal WASD strafing. Root cause: after mouse-look landed, `KeyD` still mapped to yaw, so W+D diagonal input turned the camera by `0.72` radians instead of strafing; this made controls feel pre-FPS and left diagonal normalization unproven. Added a RED dogfood regression for W+D: same distance as W-only, positive lateral movement, heading unchanged. Runtime now maps A/D to strafe vectors, normalizes forward+strafe input by vector length, keeps arrow keys as keyboard turning, and updates HUD guidance. Browser probe returned forward distance `0.9`, diagonal distance `0.9`, heading `0`, blockedReason `null`; screenshot `/Users/joehermesbot/.hermes/cache/screenshots/browser_screenshot_d1558afc98974abdba6ca5bdd8385e60.png`. Next recommended slice: task 1 mouse-look sensitivity UX polish, or task 2 key-repeat-specific regression.
- 2026-06-23 13:05 SAST: Advanced task 1 pointer-lock failure messaging. Root cause: `requestPointerLock()` was called without any rejection/unsupported-browser feedback, so denied mouse-look looked like a dead button. Added a RED server-rendered HUD regression for the unavailable state, extracted `FirstPersonMouseLookBar`, and wired `requestMouseLook` to show retry guidance on unsupported, thrown, or rejected pointer-lock requests while clearing the warning once lock succeeds. Browser denial probe showed `mouse-look unavailable`, `Mouse-look unavailable — click the city view and try again.`, and `Retry mouse-look`; screenshot `/Users/joehermesbot/.hermes/cache/screenshots/browser_screenshot_3d3953da8edc4455943730f665ede451.png`. Next recommended slice: task 1 sensitivity UX polish or task 2 diagonal/key-repeat movement polish.
- 2026-06-23 12:44 SAST: Advanced task 2 conflicting-input braking. Root cause: the first-person driver decelerated normally when W+S were held together, so an already-moving avatar coasted another `0.28` world units despite the opposite command. Added a RED dogfood regression for W then W+S; runtime now treats simultaneous forward/back as an explicit brake and zeros walk speed before movement. Browser probe returned powered distance `0.9`, conflict distance `0`, ratio `0`, `blockedReason: null`; screenshot `/Users/joehermesbot/.hermes/cache/screenshots/browser_screenshot_145ff4d810c14d6ea864c72d98581536.png`. Next recommended slice: task 1 pointer-lock denial/sensitivity UX or task 2 diagonal/turn-feel polish.
- 2026-06-23 12:21 SAST: Advanced task 3 road/path preference. Added a RED dogfood regression proving equal-time first-person road walking should cover more distance than off-road walking; it failed at equal 0.9-unit distances, then passed after `COLONY.firstPerson` gained road/off-road surface multipliers and `driveFirstPerson` applied them from the current road cell. Browser probe returned road distance `1.125`, off-road distance `0.9`, ratio `1.25`, `blockedReason: null`; screenshot `/Users/joehermesbot/.hermes/cache/screenshots/browser_screenshot_b076d98aa0d14c3796f802f0ea02bc6a.png`. Next recommended slice: task 2 diagonal/key-repeat movement polish or task 1 pointer-lock denial/sensitivity UX.
- 2026-06-23 12:02 SAST: Completed task 3 parcel-footprint guardrails. Root cause: first-person collision checks water and completed building center cells, but not the reserved parcel/lot occupancy cells that road/build placement already treats as non-walkable. Added a RED dogfood regression for a synthetic reserved adjacent parcel cell; runtime now reports `blockedReason: "parcel"` for non-road occupied cells while letting avatars already spawned inside an occupied footprint walk out. Browser probe returned position `{x:251.45,y:98}` unchanged and `blockedReason: "parcel"`; screenshot `/Users/joehermesbot/.hermes/cache/screenshots/browser_screenshot_060847cd1e7e4d1da4468cffdb4d4664.png`. Next recommended slice: road/path preference in first-person movement, or task 2 diagonal/key-repeat polish.
- 2026-06-23 11:43 SAST: Advanced task 3 building collision guardrails. Added a RED dogfood regression that places Joe beside a synthetic completed habitat cell, walks forward, and expects the live first-person position to stay put with `blockedReason: "building"`; runtime `blockedStepReason` now treats completed building cells as blocked after water/edge checks. Browser probe returned position `{x:251.45,y:98}` unchanged and `blockedReason: "building"`, with HUD screenshot `/Users/joehermesbot/.hermes/cache/screenshots/browser_screenshot_ddcb8136e2284047a588fe191ef74c10.png`. Next recommended slice: extend task 3 to shop/parcel footprints or road/path preference, or return to task 2 diagonal/key-repeat movement polish.
- 2026-06-23 11:24 SAST: Completed task 5 photo/demo capture. Added `ColonyRuntime.captureFirstPersonDemo()` so browser automation gets a deterministic public-safe evidence payload alongside a renderer PNG data URL: citizen id/name, rounded live position, heading, look pitch, action prompt, blocked reason, clock label, and player HUD lines. Browser probe after stepping Joe returned action `Talk to KOOKER the Builder`, position `{x:217.97,y:336.97}`, heading `1.608`, lookPitch `0.025`, and a PNG data URL; screenshot `/Users/joehermesbot/.hermes/cache/screenshots/browser_screenshot_7a7e655a573b435888cb4ff1127cb609.png`. Next recommended slice: return to task 3 building/shop collision guardrails, or task 2 diagonal/key-repeat movement polish.
- 2026-06-23 11:06 SAST: Completed task 6 immersive HUD split. Added a RED server-rendered regression that the first-person panel keeps player-facing Action/Blocked status visible while hiding Ground/Neighbours debug telemetry by default, then moved ground/time/nearby diagnostics behind a Show debug/Hide debug operator toggle. Browser demo verified the collapsed HUD shows Action + Show debug without debug telemetry and the toggle expands Ground/Time/Neighbours only on demand. Screenshots: `/Users/joehermesbot/.hermes/cache/screenshots/browser_screenshot_85ea047aa1f84bd78fa42ff0ecc5c77d.png` collapsed, `/Users/joehermesbot/.hermes/cache/screenshots/browser_screenshot_be587c7e9b9c463685f4fd229ff8e38d.png` expanded. Next recommended slice: task 5 deterministic photo/demo capture and PR/tasklist publication flow, or return to task 3 building/shop collision guardrails.
- 2026-06-23 10:47 SAST: Advanced task 1 pointer-lock/mouse-look. Added a RED regression for first-person mouse-look deltas, then implemented `applyFirstPersonMouseLook(dx, dy)` with tunable sensitivity and pitch clamp, fed the pitch through the renderer camera, and added a HUD pointer-lock button. Browser probe confirmed pointer lock landed on `.canvas-host`, mouse-look changed heading `1.5708 -> 1.8708` and pitch `0 -> 0.6`, and Escape released pointer lock while first-person stayed active; screenshot `/Users/joehermesbot/.hermes/cache/screenshots/browser_screenshot_825bcd306e37400ba3487a3d96fbdfeb.png`. Remaining task-1 work: sensitivity/polish and pointer-lock denial messaging.
- 2026-06-23: Advanced task 3 water guardrails. Added a RED dogfood regression that places first-person Joe at a deterministic land/water edge and asserts a forward step stays on land while reporting `blockedReason: "water"`; runtime now exposes water/edge-of-map blocked reasons through `ColonyUiState.firstPerson`, the HUD renders `Blocked water`, and `placeFirstPersonDogfood` gives future dogfood/browser probes a public placement hook. Verified focused Vitest, full Vitest, typecheck, build, and browser screenshot `/Users/joehermesbot/.hermes/cache/screenshots/browser_screenshot_337be0b60f4e46e9bd98a45bfb2da9cc.png`. Remaining task-3 work: building/shop collision, road/path preference, and richer blocked reason taxonomy.
- 2026-06-23: Advanced task 2 smooth movement key handling. Added a RED dogfood regression proving `setFpKey("KeyW")` did not move the first-person avatar, then fixed the runtime input map to accept browser `KeyboardEvent.code` names directly (`KeyW/A/S/D`) in addition to normalized keys. Verified focused Vitest, full Vitest, typecheck, build, and browser smoke screenshot `/Users/joehermesbot/.hermes/cache/screenshots/browser_screenshot_a1c84bcb0f454586a8bb2c481ecb9786.png`. Browser smoke moved Joe 0.9 world units via direct `KeyW` runtime input. Remaining task-2 work: explicit diagonal/key-repeat regression and any strafe-vs-turn decision.
- 2026-06-23: Advanced task 2 smooth movement. Added `COLONY.firstPerson` locomotion tunables, replaced instant first-person speed with deterministic ramp-up/coast-down velocity state, and reset walk speed on enter/exit/collision. Added a dogfood regression that failed against instant 3.4 units/sec movement, then passed after the fix. Verified focused first-person tests, full Vitest, typecheck, build, and browser smoke screenshot `/Users/joehermesbot/.hermes/cache/screenshots/browser_screenshot_88c12f425de44acdab02afeecc979563.png`. Remaining task-2 work: explicit diagonal/key-repeat regression and any strafe-vs-turn decision.
- 2026-06-23: Completed task 7, route dogfood. Added `src/colony/bot/firstPersonDogfood.ts` and `tests/firstPersonDogfood.test.ts` so a deterministic scripted first-person route now drives W/D/S via the runtime key path and asserts before/after position, heading, and live view samples. Verified with focused Vitest, full Vitest, typecheck, and build. Next recommended slice: task 2 smooth movement with acceleration/deceleration, or task 1 pointer-lock/mouse-look if the branch should move into browser controls next.
- 2026-06-23: Completed task 4, interaction affordance. Added a deterministic `interactionPrompt` to the first-person JSON view, rendered it in the HUD, and captured `/Users/joehermesbot/.hermes/cache/screenshots/browser_screenshot_15f2b3657a894e379e149d378c8e7e5d.png`. Branch note: PR #69 is already merged and remote branch was deleted; continued on recreated `joe/first-person-live-position` from `origin/main` so follow-up work stays isolated and unmerged. Next recommended slice: task 7 route dogfood or task 2 smooth movement math.
