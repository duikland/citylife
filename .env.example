# Copy to .env.local and set your kooker APISIX gateway base URL.
# The browser calls /kooker/... which Vite proxies to this gateway (kooker-service-user etc.).
KOOKER_GATEWAY=https://your-kooker-gateway.example.com
