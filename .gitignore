node_modules
dist
dist-ssr
*.local
.vite
.DS_Store
*.log

# local marketing/export artifacts (cards, frames) — not part of the build
social-export/

# scratch commit-message file written by Write before git commit -F
.git-commit-msg.tmp

# operator bot-provisioning helper (reads secrets at runtime; not part of the app)
provision-citizen.sh
